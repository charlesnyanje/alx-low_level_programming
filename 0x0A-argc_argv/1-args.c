#include <stdio.h>

/**
 * main - prints the number of arguments passed into it.
 * @argc:function param
 * @argv:param
 * Return: 0 (success)
 */

int main(int argc, char *argv[])
{
	argv = argv;

	printf("argc = %d\n", argc - 1);
	return (0);
}
