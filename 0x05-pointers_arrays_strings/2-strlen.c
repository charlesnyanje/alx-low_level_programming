#include "main.h"
#include <string.h>

/**
 * _strlen - entry point into the program
 * @s: function parameter
 *
 *Return: the lenght of the string
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
continue;
}
return (i);
}
