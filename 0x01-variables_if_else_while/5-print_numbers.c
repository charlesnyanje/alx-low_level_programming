#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always (0) success
 *
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("%c", '\n');
	return (0);
}
